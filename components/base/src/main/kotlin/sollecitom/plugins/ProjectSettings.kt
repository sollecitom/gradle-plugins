package sollecitom.plugins

/** Root-level project coordinate constants used across convention plugins. */
object ProjectSettings {
    const val rootGroupId = "sollecitom"
    const val groupId = "$rootGroupId.gradle-plugins"
}